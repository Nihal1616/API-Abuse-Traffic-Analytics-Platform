# Rate limit zones and HTTP-level settings (must live in http context)

# Define rate limit zones
limit_req_zone $binary_remote_addr zone=api_global:10m rate=30r/m;
limit_req_zone $binary_remote_addr zone=auth_endpoints:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=admin_endpoints:10m rate=50r/m;
limit_req_zone $binary_remote_addr zone=strict_zone:10m rate=10r/m;

# Define connection limit zones
limit_conn_zone $binary_remote_addr zone=addr:10m;
limit_conn_zone $server_name zone=perserver:10m;

# Connection limits (http-level directive)
limit_conn addr 10;
limit_conn perserver 100;

# Request body size limit
client_max_body_size 10m;

# Buffer size for request body
client_body_buffer_size 128k;

# Timeouts
client_body_timeout 12;
client_header_timeout 12;
send_timeout 10;
